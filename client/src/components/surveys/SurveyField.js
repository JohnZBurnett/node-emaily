import React, { Component } from 'react';

// SurveyField contains logic to render a single label and text input
export default () => {
    return(
        <div>
            SurveyField
        </div>
    )
}